<h2>New book</h2>
<%= form_for @booknew do |f| %>
	<%= f.label :title %><br>
	<%= f.text_field :title %>
	<%= f.label :Opinion %>
	<%= f.text_area :body %>
	<%= f.submit 'Create Book', class:'btn btn-info' %>
<% end %>