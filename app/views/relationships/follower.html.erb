<div class='container'>
	<div class="row">
		<h2>フォロー中</h2>
		<% @users.each do |user| %>
			<%= attachment_image_tag user, :profile_image, fallback: 'noimage.png', size: '30x30' %>
			<%= user.name %>
		<% end %>
	</div>
</div>